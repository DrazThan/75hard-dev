# PostgreSQL connection string
DATABASE_URL=postgresql://postgres:password@localhost:5432/75hard

# JWT — generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change_me_generate_a_real_secret

# Fernet encryption key — generate with:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=change_me_generate_a_fernet_key

# Optional: GitHub personal access token (raises rate limit from 60 → 5000 req/hr)
GITHUB_TOKEN=

# GitHub username to auto-check daily commits against (non-work repos only)
GITHUB_USERNAME=DrazThan
